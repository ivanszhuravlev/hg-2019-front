import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    closeButton: {
        position: 'absolute',
        top: 0,
        right: 0,
        marginTop: 12,
        marginRight: 12,
    },
    modal: {
        paddingTop: 40
    }
});

export default styles;