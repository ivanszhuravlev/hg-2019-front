import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    listWrap: {
        backgroundColor: "#eeeeee"
    },
    list: {
        flexDirection: 'column',
        width: '100%',
        height: '100%',
    }
});

export default styles;